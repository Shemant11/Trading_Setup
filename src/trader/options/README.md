# trader.options

Options analytics used by both the option strategies and reporting.

* `greeks.py` — Black-Scholes prices + greeks + Newton implied volatility.
* `iv_rank.py` — Per-underlying rolling IV Rank + IV Percentile + expected move.
* `chain.py` — Max pain + OI-build-up classifier.
