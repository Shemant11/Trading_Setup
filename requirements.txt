# ============================================================================
# Institutional-Grade Indian Market Trading System
# Windows-only requirements file
# ----------------------------------------------------------------------------
# Python: 3.12 or newer  (https://www.python.org/downloads/)
#
# One-shot setup on a fresh Windows PC (PowerShell, from repo root):
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip setuptools wheel
#   pip install -r requirement.txt
#   pip install -e .
#   python install.py
#
# If PowerShell blocks the activate script, run once per user:
#   Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
#
# Native build tools required for lightgbm / xgboost / argon2-cffi:
#   Install "Build Tools for Visual Studio 2022" (C++ workload)
#   Install "Microsoft Visual C++ Redistributable 2015-2022 x64"
# ============================================================================

# --- Web / API -----------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
httpx>=0.27.2
websockets>=13.1

# --- Config / Models -----------------------------------------------------------
pydantic>=2.9.0
pydantic-settings>=2.5.0
pyyaml>=6.0.2

# --- Data ----------------------------------------------------------------------
polars>=1.10.0
pyarrow>=17.0.0
pandas>=2.2.3
numpy>=1.26.4
scipy>=1.14.1

# --- Storage -------------------------------------------------------------------
sqlalchemy>=2.0.35
alembic>=1.13.3
aiosqlite>=0.20.0
redis>=5.1.1

# --- Scheduling / Time zones ---------------------------------------------------
apscheduler>=3.10.4
pytz>=2024.2
python-dateutil>=2.9.0
tzdata>=2024.2

# --- Observability -------------------------------------------------------------
structlog>=24.4.0
rich>=13.9.2
prometheus-client>=0.21.0

# --- Security ------------------------------------------------------------------
cryptography>=43.0.1
argon2-cffi>=23.1.0

# --- Machine Learning ----------------------------------------------------------
scikit-learn>=1.5.2
lightgbm>=4.5.0
xgboost>=2.1.1
mlflow>=2.16.2

# --- CLI / Utilities -----------------------------------------------------------
typer>=0.12.5
tenacity>=9.0.0
orjson>=3.10.7

# --- Testing / Linting (dev) ---------------------------------------------------
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
respx>=0.21.1
freezegun>=1.5.1
hypothesis>=6.112.4
ruff>=0.6.9
mypy>=1.11.2